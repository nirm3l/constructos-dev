# Runtime profile
DEPLOY_TARGET=auto
DEPLOY_SOURCE=ghcr
IMAGE_TAG=v0.1.230

# GHCR image location
GHCR_OWNER=nirm3l
GHCR_IMAGE_PREFIX=constructos

# Security tokens (change these values)
MCP_AUTH_TOKEN=replace-with-strong-random-token
MCP_TOOL_AUTH_TOKEN=replace-with-strong-random-token

# Optional licensing token from your control-plane
LICENSE_SERVER_TOKEN=

# Optional service credentials overrides
# POSTGRES_DB=task_management
# POSTGRES_USER=m4tr1x
# POSTGRES_PASSWORD=change-me
# NEO4J_AUTH=neo4j/change-me
